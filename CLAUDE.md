# Eternity II Solver

## Goal

Build an Eternity II puzzle solver from scratch in this repo to **beat the current records**:

- **5-clue variant**: record is **464/480 (2026-07 forum post, e2lib-verified: clean multiset, all 5 clues, 0 border errors on all three published boards)** → need ≥465. Seeds: `runs/seed_5clue_464.txt` (basin A = the post's "Best"/#1 board) + `runs/seed_5clue_464_v2.txt`/`_v3.txt` (basin B, 6-cell drift siblings; A↔B differ at 250/256 cells — two independent 464 basins). `runs/best_c5.txt` no longer holds the 461 — as of 2026-07-10 it holds a 432 from-scratch board (from-scratch runs overwrote it); solver-format 464 file staged at `runs/best_c5_464_v3_staged.txt` — adopt by copying it over best_c5.txt, or run once with `--seed-edges runs\seed_5clue_464_v3.txt` (report_completion then writes best_c5.txt itself). Old 461 seed kept in `runs/seed_5clue_461.txt`. Our best from scratch: **455** — the 2026-07-11 7.27-h mix lineage (`runs_scratch\best_c5.txt`, e2lib-verified 2026-07-12: 455, 0 border errors, clean multiset, all 5 clues; reached 453 in 32 min, 455 at ~2.6 h, then flat — plateau is polish/merge-stagnation-limited, not hunter-limited) plus five distinct same-score 455 siblings from plateau merges (`runs\candidate_merge_455_*.txt`). The dedicated pipeline to push this lineage to ≥460 is `push460.ps1` (see below). Best single-90-s-run: **451** — hit twice independently on 2026-07-11 in the `--scan-orient` A/B (90-s hunt runs under the slip-460 stack, orientations 1 and 2), both e2lib-verified, agreeing on only 6/256 cells (independent basins): `eval/best_c5_451_scratch.txt` (orient 1) and `eval/best_c5_451_scratch_b.txt` (orient 2). Previous: 449 ×2 (`eval/best_c5_449_scratch.txt`/`_b.txt`, agreeing on 10/256), 448 (`eval/best_c5_448_scratch.txt`), 446 (`eval/best_c5_446_slip.txt`), 433 (`eval/best_c5_433.txt`).
- **1-clue variant** (center clue only, piece 139): record is 470/480 → need ≥471. Best so far: 470 (`runs/best_c1.txt`), seeded from the public Joshua_Blackwood_470 board (`runs/seed_Joshua_Blackwood_470.txt`, loaded via `--seed-edges <url>`).

Constraints/preferences:
- Any language/technique allowed. GPU (CUDA, RTX 2080 Ti) explicitly deferred as future work.
- Runs on the user's own machine: Windows 11 Pro, Ryzen 9 3950X 16C/32T, RTX 2080 Ti. Windows Smart App Control was permanently disabled by the user (it blocked unsigned exes).
- Every result must be output as a valid https://e2.bucas.name/ puzzle link (the solver writes one into `runs/best_c{1,5}.txt` and `runs/history_c{1,5}.log` on every new best).
- Long hunts: `scripts\run5.ps1` / `run1.ps1` (auto-resume from `runs\best_c*.txt`; `-Minutes N` for timed runs). Build with `scripts\build.ps1`.
- **Root launcher `run.ps1`** (`run.cmd` shim for fresh machines): interactive from-scratch/from-best runner — prompts for run type, clues (1/5), threads, minutes (blank = indefinite), stagnation hours (blank = never) + on-stagnation action; builds solver.exe if missing; from-scratch uses `runs_scratch\` (gitignored, offers archive-and-restart if a scratch best exists; archiving also moves the drift dir so stale snapshots can't poison the next lineage's merges), from-best resumes `runs\best_c{1,5}.txt`; drift to `<outdir>\drift`. Console is copied to `<outdir>\run_c<clues>_<ts>.log` via `Start-Process` stdout redirect + tail loop (plain ASCII bytes; the old `Tee-Object` wrote UTF-16) — the launcher watches `best_c<clues>.txt`'s mtime, and `-StagnationHours N` kills the solver after N hours with no new best (safe: every best is already on disk), then `-OnStagnation` picks `stop` (default) / `merge` (runs `scripts\merge.ps1`) / `restart` (scratch: archive best+history+drift and loop with a truly fresh run; best: relaunch resuming the same best file, pools reset). `-OutDir` overrides the output dir. Non-interactive: `.\run.ps1 -Mode scratch -Clues 5 -Threads 30 -Minutes 90 [-Fresh] [-StagnationHours 3 -OnStagnation restart]`. **The `$ScratchArgs`/`$BestArgs` blocks in `scripts\flags.ps1` (dot-sourced by run.ps1 AND push460.ps1; moved out of run.ps1 2026-07-12) are the canonical best-known flag sets — whenever a flag config is proven better (A/B tested, e2lib-verified), updating that file is part of adopting it.**
- **Plateau-merge launcher `scripts\merge.ps1`**: wraps `tools/plateau_merge.py` with the proven args (`--time-per-pair 60 --time-top 600 --workers 16 --max-window 40`, 1800-s budget) on `<OutDir>\best_c<Clues>.txt` + `<OutDir>\drift` (defaults `runs_scratch`/5; `-Loop` for continuous harvest alongside a running solver). New `--include FILE` in plateau_merge.py injects the best board itself into the snapshot pool (score recomputed via e2lib, not trusted); since 2026-07-15 the pool also reads `<drift>\plateau.txt` (the solver's persisted g_plateau dump) and the TOP score group gets `--time-top` (default 600 s) per pair while lower groups keep `--time-per-pair` (evidence: 22-cell 454-pairs decided in 20-32 s, 21+-cell pairs went UNKNOWN at 60 s). Improvements are e2lib-verified and land in `runs\candidate_merge_<score>_<hash8>.txt` (hash suffix so distinct same-score boards don't clobber). First real invocation (2026-07-11, on the 453-lineage drift files in `runs_scratch\drift`) merged two different 452 pairs into two distinct 453s within its budget.
- **460-from-scratch pipeline `push460.ps1`** (`push460.cmd` shim; built 2026-07-12 from the strategy analysis: from-scratch plateaus are polish/merge-stagnation-limited, and our own 455 boards had never been CP-SAT-probed): one console orchestrating three engines + glue. (1) solver.exe resuming `runs_scratch\best_c5.txt` under `$ScratchArgs` with `--drift-cap 4096` (new solver flag, default 64 = old behavior — max snapshot lines per `drift_t<id>.txt`, O(1) append below cap via cached line count); (2) `plateau_merge.py --loop` (new `--max-boards` cap, default off, merge.ps1/push460 pass 500: subsamples each score group so pass startup stays O(n²)-bounded; dedupe now O(n) and state-fingerprint check happens BEFORE the 256-cell diff); (3) `window_sweep.py` rotated over the best board + every `candidate_merge_*` ≥ best−1, copied to immutable `runs_scratch\sweep\target_<score>_<hash8>.txt` targets, in resumable 45-min chunks (pass 1 = 300 s/window, pass 2 = `--retry-unknown` at 600 s; `.pass{1,2}.done` markers; **a budget-exhausted chunk prints BOTH "time budget exhausted" and the "done:" summary — check the budget marker first or a pass gets retired after one chunk, bug fixed in Finish-Sweep**). Glue (30-s poll): ADOPT any merge/sweep candidate scoring > current best (restart solver `--seed-edges <candidate>`; solver verifies conformance; 2-attempt cap per file), stagnation restart after `-StagnationHours` (default 3 since 2026-07-15 — restarts no longer lose the plateau pool, and the restart is **dump-then-merge**: wait ≤6 min for a plateau.txt dump newer than the trigger, then ≤30 min for one full merge pass that started after it, then restart; a new best or an adoption while waiting cancels it; `-DriftEvery 0` restores the old immediate restart), stop at `-Target` (default 460) printing the bucas URL. **Basin-exhaustion detector (2026-07-13)**: no new best for `-ExhaustedHours` (default 6, 0 = off; clock = last actual best improvement, survives solver restarts) AND sweep queue drained (no chunk running, no target left at ≥ best−1; any new merge candidate re-fills the queue, so drained really means merge+sweep are out of ideas on this basin; with `-NoSweep` the check is hours-only) → `-OnExhausted notify` (default: yellow banner + console beep, re-fires hourly, run continues and stagnation restarts stay active) or `-OnExhausted restart` (stop children FIRST — open handles block Move-Item on drift/ — then Archive-Lineage (the -Fresh block, now a function), reset `$CandidateCutoff`/AdoptTried/exhaustion clocks, relaunch solver+merge on a truly fresh basin, then `continue` the poll since `$cur` is stale pre-archive). Both prompted for in interactive mode; both paths smoke-tested 2026-07-13 (temp OutDir seeded with the 455 board, `-ExhaustedHours 0.01`, solver-only — notify banner fired and run continued; restart archived best/history/drift and the fresh lineage hit 445 within a minute). `-Fresh` archives best/history/drift/sweep AND ignores older `runs\candidate_merge_*` (they'd re-adopt the old basin over the new one). `-Minutes N` for smoke runs; `-NoMerge -NoSweep` = solver only (no python needed). **Interactive startup like run.ps1** (2026-07-12): zero parameters (or double-clicking push460.cmd) prompts for continue/archive lineage, total thread budget, target, minutes, stagnation hours, build-native-vs-portable exe (only when solver.exe is missing), and offers a solver-only run instead of throwing when python/ortools is missing; passing ANY parameter = scripted mode, no prompts (gate: `$PSBoundParameters.Count -eq 0`). Thread defaults auto-scale: `-TotalThreads` (or the prompt) sets the budget (default = logical cores − 2, headroom for OS + orchestrator), split 62/19/19 % solver/merge/sweep, min 2 each (= 18/5/5 on the 32-thread reference box); `-SolverThreads`/`-MergeWorkers`/`-SweepWorkers` override slices. Python detection (push460.ps1 AND scripts\merge.ps1, 2026-07-12): probes `python` then the `py` launcher via `cmd /c "<cand> -c "import ortools"" >nul 2>&1` — some installs only have `py`; the cmd wrapper keeps failed imports silent (PS 5.1 wraps native stderr in ErrorRecords, which `$ErrorActionPreference=Stop` escalates) and the Microsoft Store `python` alias stub fails both probes and falls through; whichever candidate wins is what Start-Merge/Start-Sweep launch. Smoke-validated 2026-07-12 (4-min run): merge loop produced two NEW distinct 455s (`candidate_merge_455_4279accd/addb1f31`) within 90 s of starting — the bigger pool pays immediately. **PS 5.1 gotcha found here: local `$target` collides case-insensitively with the `[int]$Target` param → int-cast crash; locals renamed.**
- **User starts long runs themselves** — do not launch unattended background hunts without asking.

## Solver architecture

`src/solver.cpp` (single file, C++20, ~1000 lines) — multithreaded (default cores−2), default `--mode mix`.

**Hunter** (full-board constructor):
- Randomized *perfect* border-ring DFS, then row-major interior DFS under a mismatch budget: exact candidate buckets `[west][north]` + mismatch lists, color-deficit lower bound, perfect-prefix rows with `pre_budget`.
- Anytime B&B: each completion tightens budget to `cost−1`; self-tightening global budget. Also `greedy_complete()` (min-mismatch per cell, ~390–405 score) for fast pool bootstrap.

**Polisher (LNS)** — takes a completed board and repeatedly:
- Re-solves regions *exactly* via `RegionSolver` B&B: 50% rectangles (sides 2–5) / 50% fault-blobs, up to `region_max=16` cells, `region_node_cap=2M`.
- **Incumbent seeding (crucial)**: B&B starts with the original arrangement as initial best (`best_gain = inc_gain − 1`), so capped searches can never lose ground, and pruning is ~1000× stronger (exhaustive to n≈16). Ties may replace incumbent → deliberate sideways drift (per-node candidates sorted by immediate gain desc, random tie-break; per-candidate break bound `gain + cand.g + suffix_max[i+1] <= best_gain`).
- Hungarian O(n³) optimal reassignment of pairwise non-adjacent fault cells ("scatter").

**Two-tier seed pools** (fixes lineage trapping): `g_seeds` elite (score ≥ best − seed_slack) + `g_fresh` (all completions). `ensure_board`: 30% fresh / else elite / else fresh / else global best. **Gotcha fixed once already**: mix_thread's bootstrap gate must check `g_fresh.size() < 8`, not `g_seeds` — gating on the elite pool deadlocks polish forever when resuming a high best (greedy completions can't reach the elite threshold).

Other facts:
- Clue cells pinned, rim always grey-correct → every reported score is record-eligible. `board_conforms()` enforces this on resume/seed.
- `--seed-edges` accepts a full bucas URL or bare 1024-letter string.
- `--minutes N` watchdog for timed runs; status line every 15 s (best/restarts/avgdepth/nodes/polish/seeds/fresh).
- Validation: `tools/e2lib.py` independently reproduces C++ scores exactly (piece multiset, clue placement, grey border all cross-checked).
- Build: `scripts\build.ps1` (g++ full WinGet WinLibs path, `-O3 -march=native -funroll-loops -std=c++20 -static -pthread`). **Portability (2026-07-12)**: `scripts\build.ps1 -Portable` builds `bin\solver.exe` with `-march=x86-64-v3` (any AVX2 CPU ~2015+; static, 3.5 MB, committed to the repo) — 15-s A/B showed no throughput cost vs native (203 vs 206 M nodes/s). run.ps1 resolves the exe as: root `solver.exe` (native, now gitignored) → try build.ps1 → fall back to `bin\solver.exe`, so a fresh clone runs hunts with **no gcc and no python** (verified by hiding exe+build script). Python remains needed only for the optional offline plateau merge (`scripts\merge.ps1`) and dev/verification tooling. **After changing src\solver.cpp, rebuild BOTH exes (`build.ps1` and `build.ps1 -Portable`) and commit the refreshed bin\solver.exe.**

Tuning ideas not yet done: full per-node Hungarian bound in RegionSolver (only the top-2 dynamic suffix bound is in); GPU region B&B. (polish_frac tuning was done 2026-07-12 as `--polish-dyn` — A/B neutral, see entry below.)

**2026-07-10 Verhaard adoptions** (from analyzing https://www.shortestpath.se/eii/eii_details.html — Louis Verhaard's 2008 prize-winning 467 solver writeup; all in `src/solver.cpp`, all opt-in flags except the mix-thread fix):
- **Graded slip schedule (`--slip-target N`, THE from-scratch breakthrough)**: replaces the `free_row`/`pre_budget` step function with LV's cumulative per-depth mismatch caps — perfect prefix for the first ~70% of the scan, then caps rise 1 at a time with shrinking gaps (sqrt spacing) across the last `--slip-span` (0.30) of the scan, topping out at `480−N`; the last ~span/10 cells get no new slips (LV holds the final 16 cells). Under slip, mismatch lists are gated only by per-depth headroom (no exact-empty requirement) and the global budget is clamped to `480−N`. **90-s from-scratch smoke (hunt, order=model): target 440→best 440, 445→443, 450→444, 455→445, 460→446, 465→436, 470→438 vs baseline 432; sweet spot `--slip-target 460`.** The 446 (e2lib-verified, `eval/best_c5_446_slip.txt`) beats the old all-time from-scratch 433. Mechanism: most restarts die at depth ~4 (~200k restarts/90 s), but survivors carry a ≤20-mismatch prefix to depth ~182 and the hybrid greedy tail closes the rest. Mix-mode 2.5 min: 444 vs 439 baseline.
- **Mix-thread hunt turn is now a node budget, not one restart** (default behavior change, all modes): under slip, restarts die in microseconds, and the old one-restart hunt turn starved hunters against multi-second polish blocks (mix best collapsed to 420/maxdepth 14). A turn now loops `run_restart()` until `node_cap` nodes are spent — identical to old behavior when restarts exhaust the cap.
- **Adaptive restart aborts (`--abort-points "nodes:mindepth,..."`)**: LV's factor-5 heuristic; kills a restart whose `max_depth` lags a node-indexed milestone. With `"2000000:60,10000000:120"` on top of slip-460: same 90-s best (446) at **+50% restarts** (304k vs 200k) — more lottery tickets per hour. Recommended companion to slip.
- **Good groups (`--groups-file`, generator `--mode make-groups`)**: piece-tileability classes per LV — group tileability = #ways 6 distinct group pieces fill a 3×2 interior rectangle (7 internal edges matched; adaptation: our ring is solved separately, so groups are interior-only). `--mode make-groups` hill-climbs 60 groups of 98 (random ~6.5M → ~26M tileability, ~15 min all-cores at `--groups-iters 6000`) → `eval/groups.txt`; precious/useless = top/bottom-16 by cross-group frequency. **LV's hard depth gates are LETHAL here** (his 63/79/96/100 rescaled → avgdepth 7 vs 98, best 427; even useless-forcing alone → avgdepth 63; gates now default off, opt-in via `--group-cks A,B,C,D`). Shipped adaptation: **class-priority bucket ordering** (useless>bad>good>precious, dominant sort key, `--group-order 1` default) — spend worst-tiling pieces early without pruning legal moves. Smoke: neutral (432/94/190 vs 432/98/189 baseline; under slip 444 vs 446, within noise) — harmless, unproven; the honest test is deep-completion frequency in long runs.
- **Column-wise tail scan (`--tail-cols N`)**: LV's optimized search orders end in a column-swept strip. Solo: neutral (433). Under slip: **hurts** (433, maxdepth 142 — tail cells face two decided neighbors exactly where the slip allowance lives, branching dies). Not recommended as-is; LV co-optimized order+slip jointly via a Markov model of per-depth branching (his appendix has the DP pseudocode), which is the missing piece if revisited (Track A logs = the data source).
- **For long runs**: recommended from-scratch/fresh-lineage config = `--slip-target 460 --abort-points "2000000:60,10000000:120"` (scripts unchanged — record hunts resume high seeds where the hunter's job is pool food; slip makes that food 432→446, so likely also right for run5/run1, but only smoke-tested from scratch). LV's remaining unadopted ideas: restart criteria tied to *solution rate* (needs completions to count), order/slip co-optimization model, most-visited-depth abort signal.

**2026-07-11 `--theft-guard` A/B — NEGATIVE, leave off** (raphael-anjou wiki item 2, WATERSHED/LODESTONE finding: ~90% of DFS deaths are a (W,N) demand whose unique serving piece was spent earlier; flag in `Buckets::build` subtracts ALPHA × #uniquely-served exact demands from each candidate's sort key so unique servers are consumed later): 90-s from-scratch A/B on `--clues 5 --mode hunt --order model --model-file eval\model_weights.txt --slip-target 460`, 3 runs/arm interleaved B,T¼,T1,… for thermal fairness, fresh out dirs (`runs_scratch\ab_theft\`), every best board e2lib-verified (score match, 0 border errors, clean multiset). **Baseline: 448/444/447 (mean 446.3; restarts 205k/193k/199k; maxdepth 183/180/184). α=0.25: 446/447/446 (mean 446.3; restarts 197k/185k/189k). α=1.0: 444/446/443 (mean 444.3; restarts 193k/191k/193k).** Verdict: α=0.25 exactly neutral (a prior-session triple `runs_scratch\tg025_*` agrees: 447/445/446); α=1.0 is a −2.0 mean regression right at the pre-registered threshold, plus both arms shave ~3–4% restarts. Effect is ≤0 and monotonically worse with alpha → **no alpha sweep; default stays 0 (off)**. Likely reason: the existing color-deficit bound + scarce-color ordering already cover most theft deaths, and demoting unique servers fights the learned `--order model` ranking. Side findings: (1) 90-s baseline best-score spread is 444–448 — ±2 is noise, wider than the earlier "446 rock-stable" read; single-run smoke diffs of ±2 prove nothing. (2) Baseline run B1 hit **448 = new from-scratch 90-s best** (no theft guard, plain slip-460 flags), saved to `eval/best_c5_448_scratch.txt`.

**2026-07-11 `--hybrid-min-depth` A/B — NEUTRAL, default stays 0** (gate hybrid emission on the restart's deepest prefix depth; hypothesis: under slip ~89% of restarts die at depth ~4 yet emit greedy-quality hybrids that flood `g_fresh` cap 160 and evict the rare deep survivors). New instrumentation: `hybavg=` in status/final line = mean score of emitted hybrids. Calibration (60-s hunt, slip-460 stack, `--log-placements`, 89k restarts): depth distribution is a **staircase** — ~70% die at depth ≤1, ~29.5% plateau at depths 5–13, cliff to 6.75% at depth 14 (= interior row 1 done), 1.07% at 15, 0.1% at 138+; arms were gate 5 (~30% kept) and gate 14 (~6.8%). A/B (2.5-min from-scratch mix, 12T, interleaved g0,g5,g14 × 4 reps, fresh out dirs): **gate 0: 436/449/442/443 (mean 442.5); gate 5: 444/441/444/444 (443.25); gate 14: 442/445/444/443 (443.5)** — deltas ≤ +1.0 vs ±3–4 noise, sign tests nothing. hybavg 381.6→383.8→385.5: the greedy tail dominates hybrid score (14-cell perfect prefix ≈ +4 edges over 4-cell), and deep survivors were never lost anyway — `report_completion` score-gates them into `g_seeds` regardless of `g_fresh` eviction. Full writeup in SOLVER_EVAL.md Addendum 2. Windfalls: **two independent from-scratch 449s** (see Goal; one from the 60-s hunt-mode calibration run — hybrid completions make pure hunt mode a competitive from-scratch scorer).

**2026-07-11 `--ladder K` A/B — NEGATIVE, leave off (default 0)** (raphael-anjou wiki LADDER item: prefix racing / successive halving for the Hunter). Implementation: a hunt turn becomes a cohort of K independent restart prefixes run to a small node budget (`node_cap/(K*20)`); each round sorts by (max_depth desc, cost asc — fewer slips spent as the tie-break) and keeps the deeper half, doubling the node budget and replaying each survivor from its captured deepest prefix (reuses the `--hybrid` `hyb_p`/`hyb_r` machinery — new `Hunter::run_ladder`/`ladder_restore`/`ladder_finish`, `run_turn()` dispatches to it from both `hunter_thread` and `mix_thread`); the final survivor gets the turn's remaining node budget. Every cohort member, eliminated or not, ends by hybrid-completing from wherever it stopped (same gating as `run_restart`), so pool-food volume is comparable to plain restarts. 90-s from-scratch smoke (`--clues 5 --mode hunt --order model --model-file eval\model_weights.txt --slip-target 460`, fresh out dirs, interleaved baseline/K=8/K=16, all boards e2lib-verified: score matches C++, 0 border errors, clean multiset): **baseline 446/445 (mean 445.5, maxdepth 184/182, ~297M nodes/s); K=8: 431/429 (mean 430, maxdepth 144/149, ~83M nodes/s); K=16: 427/433 (mean 430, maxdepth 139/140, ~73–78M nodes/s)**. Both ladder arms land ~15.5 points below baseline and cut throughput to ~25–28% — far outside the ±3–4 noise band, so not a fluke. Root cause: **the DFS has no cheap way to resume a partially-explored subtree.** `hyb_p`/`hyb_r` only captures a single deepest *path*, not which sibling branches under it were already tried; every "extension" round re-enters `dfs()` at the captured depth with a freshly shuffled bucket order and re-explores the whole subtree from scratch. Under the slip schedule, the rare deep excursions that produce baseline's maxdepth≈184 apparently need one *unbroken* full-budget random walk rather than several independently-reshuffled looks at the same prefix — chopping that walk into rung-sized pieces and repeatedly re-entering at the same depth wastes nodes on redundant re-exploration instead of reaching new depth, which is exactly what the throughput and maxdepth numbers show. Fixing this properly would need real coroutine-style DFS suspension (saving the live recursion stack, not just the deepest path) — out of scope; not worth pursuing further given the effect size and the mechanism identified. `--ladder` is implemented and building clean (opt-in, off by default, `--selftest` unaffected) in case a future idea wants the scaffolding, but do not adopt K>=2 for hunts.

**2026-07-11 `--no-adjacent-breaks 1` A/B — NEGATIVE, leave off (default 0)** (raphael-anjou wiki item 4a: Blackwood msg 10051 discipline — reject any slip-mode mismatch placement orthogonally adjacent to a cell already involved in a mismatch, hypothesis being that the 464-record v3 board has zero double-breaks and scattered faults, so spreading faults during the hunt might yield better polish/greedy-tail food). 90-s from-scratch A/B, `--clues 5 --mode hunt --order model --model-file eval\model_weights.txt --slip-target 460`, interleaved B1,T1,B2,T2,T3,B3, fresh out dirs (`runs_scratch\ab_noadj\`), every reported board e2lib-verified (score match, 0 border errors, clean multiset): **baseline 447/448/445 (mean 446.7; restarts 197k/201k/193k; maxdepth 183/184/182); treatment 441/441/440 (mean 440.7; restarts 189k/189k/205k; maxdepth 175/177/175)**. Treatment is a consistent −6.0 mean regression, clearly outside the established ±2–4 noise band (no overlap: worst baseline run 445 still beats best treatment run 441), and maxdepth drops ~7–9 in every run regardless of restart count. Verdict: the adjacency-reject prunes real, needed slip placements — greedy/hybrid tails are unaffected (unchanged code path) so the loss is purely fewer/shallower DFS survivors, i.e. the constraint fights the slip schedule's random walk rather than helping it. The record board being double-break-free is a property of a fully-polished board reached via *unconstrained* search + LNS repair, not evidence that constraining the constructive search the same way helps find one faster. No parameter sweep — this is a hard constraint (on/off), and the single data point is unambiguous. Do not adopt; leave `--no-adjacent-breaks` at its default (0/off).

**2026-07-11 `--ring-filter PCT` A/B — NEGATIVE, leave off (default 0)** (raphael-anjou wiki item 4b: border-ring pre-filter — score each completed border ring by how well its inward-facing colors match the interior piece supply, re-roll rings scoring in the worst PCT percent). Two structural findings up front: (1) **any unweighted per-color sum over inward ring edges is ring-invariant** — every complete ring uses all 56 edge pieces and each edge piece's inward side is fixed (the side opposite its grey side), so the inward-color *multiset* is identical across rings and only the ARRANGEMENT varies; the item's suggested "sum of COLOR_RARITY over inward edges" measures a constant. (2) The `D[]`/`avail[]` deficit is identically 0 at ring time (56 inward demands spread over 17 interior colors vs ~40+ supply per color) — deficit-based ring scoring is equally uninformative. The shipped heuristic therefore scores the arrangement position-wise: ring value = Σ over the 56 inward edges of `RING_SUPPLY[color] × exp(−scan_depth/8)` (`RING_SUPPLY` = per-color side count over non-clue interior pieces; depth = row-major scan index of the edge's interior neighbor — most restarts die in scan row 1, so early-facing edges get the weight); reject below the PCT-th percentile of a rolling per-thread 1024-ring window (warm-up 256 rings, refresh every 128, kept AND rejected rings both sampled so the threshold tracks the unfiltered distribution, attempts capped at 64). Implementation: `init_ring_filter`/`ring_value` + `Hunter::solve_ring_filtered` in `src/solver.cpp`; `rings=`/`rerolls=` appear in status/final lines when on; default path bit-identical (`--selftest` PASS 383). 90-s from-scratch A/B (`--clues 5 --mode hunt --order model --model-file eval\model_weights.txt --slip-target 460 --minutes 1.5`, interleaved B,50,90 ×3, fresh out dirs `runs_scratch\ab_ringfilter\`, machine idle, every board e2lib-verified: score match, 0 border errors, clean multiset): **baseline 446/445/447 (mean 446.0; restarts 199k/205k/200k ≈ 2.24k/s; maxdepth 183/183/184); PCT=50: 445/447/445 (mean 445.7; restarts 148k/148k/150k ≈ 1.65k/s = −26%; rings/restart 1.95, rejection 48.7%); PCT=90: 444/446/442 (mean 444.0; restarts 57.8k/56.4k/57.8k ≈ 0.64k/s = −71.5%; rings/restart 8.8, rejection 88.6%)**. Verdict: PCT=50 is best-neutral while burning 26% of restart throughput; PCT=90 is a −2.0 mean at −71.5% restarts. Root cause of the overhead: the "rings are cheap vs interior DFS" premise FAILS under slip-460 — slip makes the mean restart die in ~0.45 ms while a marginal `solve_ring` costs ~150 µs (randomized 60-cell DFS with backtracking, two `mt19937_64` constructions and vector shuffles per call), i.e. one extra ring ≈ ⅓ of a restart, so the design constraint "filtering must not cut restarts by more than a few percent" is unattainable at any PCT with real selection pressure. And no per-restart enrichment shows up to pay for it: hybavg 381.6 vs 381.5–381.6, maxdepth flat-to-lower, best flat — score-vs-restarts is so flat at this level (−71.5% restarts only cost ~2 points) that any true ring-quality effect is far below measurability. Even a free filter would thus move little; a paid one loses. Flag stays in as opt-in scaffolding; do not adopt.

**2026-07-11 `--scan-orient N` A/B — POSITIVE (+3.5 mean, two all-time 451s), `--scan-orient 1` adopted for from-scratch hunts** (raphael-anjou wiki item 4c: is the Hunter's interior scan orientation an arbitrary fixed choice?). Answer: structurally yes — the row-major DFS only assumes each scan cell's W and N neighbors are decided, which holds in any of the 4 frame rotations; ring solver, buckets, deficit bound, slip schedule, and ZONE_BAND (ring-distance, rotation-invariant) are all frame-agnostic. **Structural finding**: the four outer clue cells {(2,2),(13,2),(2,13),(13,13)} are closed under 90° rotation, so orientation does NOT change the outer clues' scan-depth structure — it only cycles which clue piece sits at which depth (and its pinned rotation). The one geometric change is the center clue's cell within the central 2×2 block: orient 0→(7,8), 1→(7,7), 2→(8,7), 3→(8,8), i.e. **scan row 7 for orients 1/2 vs row 8 for orients 0/3** — and the A/B result splits exactly on that line (below), so "meet the pinned center clue one row / 14 scan cells earlier" is the working mechanism hypothesis. Implementation (`src/solver.cpp`): the Hunter runs its whole restart (ring, clue pinning, scan, hybrid/greedy tail) in a frame rotated `N`×90cw via working-frame clue tables `HUNT_IS_CLUE/HUNT_CLUE_AT/HUNT_CLUE_ROT` (`init_scan_orient()`: standard clue at s ↦ `rotate_cell(s,N)` with rot+N); every board leaving the Hunter funnels through `Hunter::emit()`/`out_board()` which un-rotates by (4−N)&3, so pools/best files/URLs are always standard-frame (score and rim/clue conformance are rotation-invariant; `board_conforms` holds on every output). Orient 0 = bit-identical old behavior (`--selftest` PASS 383, extended to verify clue-table round-trip at all 4 orientations); `--log-placements` under scan-orient warns that Track A cells are working-frame (don't mix with orient-0 training data). 90-s from-scratch A/B (`--clues 5 --mode hunt --order model --model-file eval\model_weights.txt --slip-target 460 --minutes 1.5`, interleaved B,1,2,3,B,1,2,3,B,1,2, fresh out dirs `runs_scratch\ab_scanorient\`, machine idle, every board e2lib-verified: score match, 0 border errors, clean multiset, 5 clues at standard cells): **baseline 446/446/445 (mean 445.7; restarts 192k/204k/200k; maxdepth 181/182/184); orient 1: 450/448/451 (mean 449.7; restarts 202k/206k/204k; maxdepth 184/184/185); orient 2: 448/451/449 (mean 449.3; restarts 211k/211k/216k; maxdepth 184/186/184); orient 3: 443/444 (mean 443.5; restarts 184k/177k; maxdepth 180/181; avgdepth 5 vs 4 elsewhere — restarts die deeper yet score worse)**. All six orient-1/2 runs ≥ 448 = every one ties or beats the best baseline run ever observed at 90 s (444–448 across 12 runs, mean ≈ 446); +3.5/+3.7 mean is far outside the established ±2 noise band, and 15-s smokes agreed (o1/o2 448/448, o3 433). The two 451s (orient 1 run c, orient 2 run b) agree on only 6/256 cells — independent basins, both saved to `eval/best_c5_451_scratch{,_b}.txt`. Adopted: `--scan-orient 1` added to run.ps1 `$ScratchArgs` (orient 1 vs 2 statistically tied; 1 chosen for the marginally higher mean). Untested: clues=1 (mechanism-aligned — the center clue is the only clue and moves row 8→7 — but no A/B), from-best resumed lineages (hunter output is pool food there; `$BestArgs` unchanged), and orient-1-vs-2 at longer horizons. The center-clue-row hypothesis suggests a cheaper follow-up someday: `--order`/slip co-tuning around the clue row rather than frame rotation per se.

**2026-07-11 `--band-mitm 1` A/B — NEGATIVE as a decider, leave off (default 0); correctness fully validated, scaffolding kept** (raphael-anjou improvement program item 5: exact meet-in-the-middle endgame for BandSolver bands of <=2 rows / <=32 cells). Implementation (`src/solver.cpp`, all opt-in): split the band's columns at a balanced cut xm; enumerate every left-half fill within the total budget (same tier gates as the band DFS; needs `cost2>=1`, the default) into a flat sorted table of packed u64s keyed by (<=32-bit piece mask over the freed set, <=10-bit interface signature = the 1–2 east colors on the cut), deduped to min cost per key (periodic compaction; 64M-entry/512MB cap with graceful bail-mem); stream right-half fills (leftmost-column cells take by_n/all tiers, their W edge deferred to the join) with budget tightened by the observed left minimum; join on the **forced complement mask** (left+right must partition the freed pieces — one binary search per right completion) + Hamming distance between cut signatures, anytime-tightening; a final mask-restricted replay re-materializes the winning left fill. New flags: `--band-mitm` (default 0), `--band-y0-max` (default 12 = old sweep bound; raise to 13/14 to sweep 2-/1-row bands at all — the old sweep never visited them), `--band-y0-min` clamp relaxed 12→14 (polisher rebuilds still cap y0 at 12); `--mode band` lines now print `decided=`/`ms=` and `mitm=decided|bail-mem|bail-nodes tab= joins=`; also fixed stale decided/mitm stats on the inc_cost==0 early return. `--selftest` PASS (base now reads 464: best_c5.txt holds the adopted 464; the historical "383" predates that adoption), defaults byte-identical. **Correctness**: synthetic drop-6 scramble of v3's perfect rows-13/14 (swap cells 225↔226 rot 2,2 → 458): MITM orient-3 y0=13 decided inc 6→best 0, imp=6 (joins=35, 5.4M nodes, 200 ms); repaired board e2lib-verified (464, 0 border errors, clean multiset, 5 clues) and **byte-identical to the original v3 at 256/256 cells** — join+replay reconstructs the exact optimum. DFS repairs the same damage in 5.3M nodes (orient 0) — parity. Note: the identical damage viewed in orient-0 framing made MITM bail at 30M — half-enumeration tractability is orientation-sensitive; the 4-orient sweep is what rescues it. **A/B** (2-row sweeps y0 13–14 ×4 orients, `--seed-edges`, 30M cap, fresh dirs `runs_scratch\ab_band_mitm\`): R461 — 7/8 bands decided by BOTH at comparable nodes (DFS 6–531k; MITM 6–529k, though up to ~5× worse on some easy bands, e.g. 2.3k→11.2k, because MITM must enumerate the whole half-space where DFS prunes jointly); the 8th (orient-0 y0=13, 26 free, inc 14 — the R461 fault band) undecided by both: DFS caps at 30M and 2.98G nodes (132 s); MITM bails **in the left enumeration** at 30M/300M/3G (raw fills at bail 29k–4.9M with huge shuffle-order variance, joins=0 — phase 2 never starts). 464_v3 — 7/8 decided by both (4 bands inc 0; on orient-2 y0=14 (inc 8) MITM's empty-table certificate cost exactly the DFS node count 52,660 — for a 1-row band with no left fill within budget the two trees provably coincide); orient-2 y0=13 (inc 11, the record board's fault band) undecided by both at 3G (DFS 2.88G/132 s). 433 from-scratch board — both bail on all four fault-heavy 2-row bands (inc 28/21/11/10; the inc-28 band pushed 29.4M raw left fills in just 30M nodes → also memory-bound). **Verdict/mechanism**: the join machinery is fine (and cheap — forced complement makes it one lookup per right fill), but the per-half budget ≈ the total budget because faults can concentrate on either side, so the left enumeration is itself a budget-13-ish DFS over 13 cells — empirically ≫3G nodes on every band that matters. MITM delivers its sqrt only against the *joint* tree; sqrt(intractable) is still intractable, and no sound per-half budget split exists (pigeonhole caps one side at B/2 but the streamed side keeps ~full budget). Net: **0 bands decided that DFS couldn't**, and every band MITM completes, DFS already decides in <1M nodes. Do not enable; kept as opt-in scaffolding — validated pieces (mask-partition join table, signature interface, replay materialization) could serve a future cut-based idea, e.g. an assignment/flow lower bound per half to shave budget levels, or reduced-budget partial certificates.

**2026-07-12 `--polish-dyn` (dynamic polish_frac) A/B — NEUTRAL, leave off (default 0)** (the long-listed "polish_frac tuning" idea, motivated by log analysis of a 7.27-h from-scratch 5-clue mix run, 2026-07-11, 22T, slip-460 stack, final 455: after ~15 min the Hunter contributed nothing directly — completions= frozen at the 25 greedy bootstraps across ~11.9M restarts, hybavg pinned at 381.6 over ~10.5M emissions, maxdepth flat at 183–184, ~843B hunt nodes burned while every late point came from polish drift/merge/umbrella). Implementation (`src/solver.cpp`, opt-in): `effective_hunt_share()` — a mix turn's hunt probability starts at `1−polish_frac` (0.5) and decays linearly to `--polish-dyn-floor` (default 0.15, kept nonzero because hybrids are the only g_fresh food and ensure_board draws 30% fresh) as `best_score − hybavg` moves from `--polish-dyn-gap` (default 60) to gap+10; hybavg pins at ~381.5 under the slip stack while best climbs, so the gap is a cheap hunter-uselessness proxy (1000-hybrid warm-up before any decay). Pure function of existing atomics so mix threads and the status line agree; status prints `hunt=0.NN` when on; flag off compares the RNG against `cfg.polish_frac` literally — bit-identical, `--selftest` PASS. A/B (8-min from-scratch **mix**, 12T — the 90-s hunt protocol is wrong for this feature, the useless-hunter regime needs minutes — flags per $ScratchArgs, interleaved B,T ×3, fresh dirs `runs_scratch\ab_dynpolish\`, machine idle, all six boards e2lib-verified: score match, 0 border errors, clean multiset, 5 clues; pre-registered rule: adopt only if treatment mean > baseline +4): **baseline 447/447/448 (mean 447.3; restarts 71k/77k/95k; polish iters 44.1k/47.6k/40.9k, gains +882/+899/+1054); treatment 447/446/448 (mean 447.0; hunt= settled 0.27–0.38 — partial decay, the 0.15 floor needs best ≈451.5; restarts 59k/63k/49k = −32%; polish iters 46.4k/45.8k/53.5k = +10%, gains +1059/+855/+898)**. Verdict: dead neutral (−0.3). Mechanism: BOTH sides of the reallocation are flat. Cutting hunting ~32% cost nothing — fresh pool pinned at 160 in both arms, seeds/plateau healthy, best unchanged — which *confirms* the motivating premise (hunter output past ~440 is fungible pool food). But the freed compute didn't convert either: polish gains ≈940 in both arms despite +10% iterations — at this horizon polish progress is stagnation-limited (waiting on kicks/umbrella/merge/tie-drift opportunities on a plateau), not iteration-limited, so extra polish turns re-grind the same local optima. Untested: multi-hour horizons where accumulated drift/merge volume might monetize the extra iterations (the motivating run's actual regime — an 8-min A/B can't rule it out), and from-best resumed lineages. Flag kept as opt-in scaffolding (`--polish-dyn 1 --polish-dyn-gap N --polish-dyn-floor F`); defaults and $ScratchArgs/$BestArgs unchanged.

**2026-07-15 Phase-0 (STRATEGY_460 §3-P1) 455 merge-ladder + dilate-1 cluster windows — NO 456; basin diversity exhausted at ≤28-cell diffs, ≥37-cell models CPU-undecidable ⇒ P2 harvest volume strictly dominates escalation** (compute-only campaign 2026-07-13→15, machine idle, sequential CP-SAT `window_solve.py --improve-only --workers 16`; resumable driver `tools/phase0_ladder.py` — state `docs/phase0_state.json` written after every probe, full log `docs/phase0_results.md`, raw output `runs/phase0_logs/`; the six basin-B 455s restored from git HEAD into `runs/archive_455_basinB/`, byte-verified; all 8 distinct 455s e2lib-verified: 455, 0 border errors, clean multiset, all 5 clues). Ladder = 600 s → 1800 s (UNKNOWNs) → 14400 s over the 12 cross-sub-lineage basin-B pairs (diffs 25–37; diffs <5 and cross-basin >45 skipped). Results: **(1) every diff ≤28 is INFEASIBLE** — 25–27-cell pairs in 1–5 s, both 28-cell pairs at the 1800-s tier (504/1047 s); basin-A's 4-cell pair 0.0 s. **(2) Tier-3 collapse (structural)**: the four remaining 37-cell S2×S3 pairs disagree on the *identical* 37-cell set ⇒ all four boards are identical outside it ⇒ same fixed boundary + same piece pool = ONE improve-only CP-SAT model (the hint is the only difference and improve-only discards it); the driver dedupes by model signature (window cells + outside placements + pool, md5) and propagates verdicts. One 4-h 16-worker solve: **UNKNOWN** — decides all four pairs, saving ~8 h vs the naive 3-probe tier. **(3) Dilate-1 fault-cluster windows on basin A** (both boards, byte-identical cluster geometry: 26c and 15c 8-adjacency clusters → 60c/40c windows): the 40-cell window **INFEASIBLE in 2.5 s on both** — local optimality now proven *including one ring of piece inflow*, strictly extending the 2026-07-13 bare-cluster proofs; the 60-cell window (97 win_edges) **UNKNOWN at 7200 s on both**. Decision readout (both pre-registered outcomes fire): everything decidable is INFEASIBLE ⇒ the eight 455s are merge-exhausted at their diff scale — no 456 exists among ≤28-cell merges or inflow-dilated small clusters; and the 37-cell model + both 60-cell windows are CPU-undecidable at 2–4 h ⇒ budget escalation on large windows is dead ⇒ **P2 (persist `g_plateau`, drift-not-arrival snapshots, small-diff volume) is the only path inside these basins**, and P9's big-exact-neighborhood case weakens (4-h, 49–97-win_edge models undecided). Campaign totals: 23 solves, ~11.4 h wall. Driver scaffolding is reusable for the next pool (rerun after any new same-score harvest: it skips decided probes automatically).

**2026-07-15 plateau-harvest fix (STRATEGY_460 §3 P2+P3+P5) — SHIPPED, defaults on; A/B soak prepared in `docs/phase1_soak.md` but NOT yet run (user launches)**: the measured bottleneck was harvest collapse — the on-disk pool held 2 distinct 455s while the in-memory `g_plateau` held 215 (`drift_snapshot` fired once per best-level *arrival* per thread via the `drift_last_best_seen` latch, and `g_plateau` died on every stagnation restart). Five pieces: (1) **Drift-not-arrival snapshots** (`--drift-every`, default 300, solver): while a lineage is within 1 of the global best, `bump_stagnation` snapshots every K non-improving steps, deduped by a per-thread ring of the last 256 written board hashes; the arrival snapshot stays. (2) **g_plateau persistence**: the deduped pool dumps to `<drift-dir>\plateau.txt` (drift-file format, scores recomputed) 1 min after start, then every 5 min (status-thread tick), and at clean exit; atomic tmp+rename because the merge loop reads the dir live. (3) **Startup reload**: `drift_t*.txt` + `plateau.txt` refill `g_plateau` — each line re-parsed, `board_conforms`-checked, re-scored (the file score is only a cheap pre-filter), gated `>= resumed_best − 1`, capped at `plateau_cap`; boards land in `g_plateau` ONLY (never seeds/fresh/best — no leakage path; `merge_move` re-checks scores against the live best anyway). **`--drift-every 0` restores the complete old behavior** (arrival-only snapshots, no plateau.txt, no reload) — the A/B baseline switch, exposed as push460 `-DriftEvery`; solver defaults without `--drift-dir` are bit-identical (all new code gated); `--selftest` PASS (base 464). (4) **plateau_merge.py**: pool now includes plateau.txt; per-score-group time scaling — the top score group gets `--time-top` (default 600 s)/pair, lower groups keep `--time-per-pair` 60 s (evidence in the 2026-07-13 merge log: 22-cell 454-pairs decided in 20–32 s, 21+-cell pairs UNKNOWN at 60 s — the old budget sat exactly at the decision edge); merge.ps1/push460 pass both. (5) **push460 stagnation policy**: `-StagnationHours` 2→3 + dump-then-merge restart (see the pipeline bullet above); adoption/exhaustion cancel a pending restart; Archive-Lineage/run.ps1's archiver move the whole drift dir, so plateau.txt can never poison a fresh basin. New metric script **`tools\count_distinct.py`** (distinct boards per score from a drift dir, every line e2lib re-scored, leakage + multiset + clue checks on the top-2 groups, `candidate_merge_*` counting with `--since`) — the soak readout. Both exes rebuilt (native + `bin\solver.exe`). Smoke (temp OutDir seeded with the 455, 12-thread budget, 4 min): plateau.txt appeared at minute 1; the merge loop pooled drift+plateau (8 lines) and solved the only distinct 455 pair (4-cell diff, INFEASIBLE in s); a restart printed `plateau reload: 1 boards (8 lines, gate score>=454)` → plat=2 (the other distinct board was the resumed best itself, dedup absorbed it); count_distinct: all file scores == e2lib recompute, 0 border errors, clean multisets, clues intact. Pre-registered adoption rule (phase1_soak.md): treatment disk-distinct at best/best−1 ≥ 10× baseline (~2) with clean leakage checks.

**2026-07-08 from-scratch pipeline upgrades** (all in `src/solver.cpp`; several change default behavior):
- **Umbrella bug fixed**: the stagnation resample (`--stagnation-kick`, new flag, default 4000) capped `since_gain` at 4000, so `--umbrella-stagnation 8000` could **never fire** — the umbrella kick had been dead code in every default-flag run. Now lineages at/near the global best (within 1) are exempt from the resample (`Polisher::bump_stagnation`): they keep drifting and `since_gain` keeps counting until the umbrella fires. Band-rebuild trigger changed from `== band_stagnation` to `% band_stagnation == 0` so drifting lineages re-fire it every cycle. Verified: `--stagnation-kick 150 --umbrella-stagnation 450` fires umbrella jumps with no best-score leakage.
- **Hybrid completions** (`--hybrid`, default 1): every hunter restart replays its deepest DFS prefix (captured in `hyb_p/hyb_r` whenever `max_depth` improves) and greedy-fills the tail (`greedy_fill`, refactored out of `greedy_complete`) → every restart emits a fresh-lineage completion instead of nothing. Fixes fresh-pool starvation (previously frozen at the ~8 bootstrap greedy boards, since DFS completions are needle-rare and the completion budget tightens as best rises). Smoke test: from-scratch 421/480 at 15 s, 424 at 90 s (old baseline: 410–414 after 2.5 min; the old 421 plateau took ~4 h). Status: `hyb=`/`hybavg=`; `--hybrid-min-depth` gate available but A/B-neutral (2026-07-11, default 0). Composes with `--order model` (deeper stalls → better emitted boards) — **run5/run1.ps1 now pass `--order model --model-file eval\model_weights.txt`**.
- **Pool dedup + plateau archive**: `board_hash` (FNV-1a over cell,piece,rot) dedupes all pools (`SeedPool::have`); new `g_plateau` pool (`--plateau-cap` 4096) archives every distinct board within 1 of the global best (inserted in `bump_stagnation` each step + in `report_completion`) — the 464 author's mass same-score harvest, in-process. Subsumes the old "re-add sideways-drifted equal-score boards" tuning idea. Status: `plat=`.
- **Merge move** (`--merge-pct` 2% of polish steps, `--merge-nodes` 16M): draws two plateau boards, exact-solves their disagreement window on the better one (≤40 cells; outside-window agreement ⇒ identical window piece multisets ⇒ the incumbent-seeded B&B space contains both parents' arrangements) — in-process `plateau_merge.py`. Cross-basin pairs rejected as soon as the diff passes 40 cells; pair dedup via symmetric hash set (`g_merge_tried`). Status: `merge=iters(+gains)`.
- **Tie acceptance** (`--tie-pct`, default **100**, was hardcoded 30): equal-score region/scatter/parity results are now always applied — sideways drift is score-safe (incumbent seeding) and drift speed is the plateau resource. `--tie-pct 30` restores old behavior.
- **RegionSolver dynamic suffix bound**: per-cell top-2 piece values (`cmax1/cmax2/amax_pi`); cells whose argmax piece is already used deeper in the tree contribute only their second-best value. Strictly tighter than the static `suffix_max`, enabling **big-region rolls**: `--region-big-pct` (10) % of region moves use `--region-max-big` (24) cells; `--region-max`/`--region-max-big` now clamped to ≤40 (the array cap).
- Status-line gotcha: `avgdepth` divides by *all* restarts including greedy/bootstrap completions (which contribute depth 0), so mix-mode avgdepth reads roughly half the true DFS stall depth.

**2026-07-06 additions** (all in working tree, defaults preserve old behavior unless noted):
- **Band rebuild** (`BandSolver`, macro-LNS): rebuilds interior rows y0..14 (40–96 cells) via budgeted anytime DFS with the freed pieces, budget = band faults − 1. Whole-board rotations (`rotate_board`, orient 0–3) turn column/top bands into bottom bands; clue cells stay pinned at transformed positions. Fires on polish stagnation (`--band-stagnation 3000`, `--band-nodes 30M`); `--mode band` = systematic sweep for testing; `--selftest` checks rotation identity/score invariance. Inherits the Hunter's move space (every piece matches ≥1 decided W/N neighbor), so its "exhausted = optimal" claims are relative to that space.
- **Umbrella kick** (record author's method: forced leap to distance ~32): on stagnation (`--umbrella-stagnation 8000`), destroy a random 25–40-cell blob, rebuild greedily *without* incumbent seeding, accept unconditionally. Degraded boards never leak to pools/best files (score-gated paths only). `--umbrella 0/1`, `--umbrella-dist 32`.
- **LAHC + exploration**: `--explore-pct 10`% of region moves run non-seeded (`prep_and_solve(..., seed_incumbent=false)` — may return worse); late-acceptance gate `hist[--lahc 50]` accepts/rejects with exact restore. `--lahc 0` disables both.
- **Hungarian assignment bound in RegionSolver**: per-(cell,piece) UB `val[i][p]` (best-rotation fixed-edge matches + internal edges assumed matched) tightens `suffix_max` (was: all decided edges match); root Hungarian relaxation detects tie-only regions (`amax == inc_gain`) → drift-only search at cap/16 (`tieproof=` counter in status). A/B (1 min, noisy): polish iters +41%.
- **Drift snapshots**: `--drift-dir PATH` appends `<score> <edges>` lines (per-thread ring of 64) when a polisher stalls at/near global best — food for plateau merging.
- **`--mode test-polish`**: 60-step single-polisher debug harness with trace (`p.dbg`).

**2026-07-06 raphael-anjou adoptions** (from analyzing https://github.com/raphael-anjou/eternity2, a 219-volume private research program on E2, best 463; changes default behavior where noted):
- **Cost-2 double-break tier** (`--cost2`, default **1**): `Buckets::all_list` + a budget-gated third `try_list(..., true, true)` tier (both W and N mismatch). Motivation: REPLAY experiment proved community strict-460+ boards contain double-break cells one-break-per-cell search can't construct; verified on our boards (JB470 has one at (2,10), R461 at (14,14)) while our old lists (`exact`/`by_w`/`by_n`) could never place one. `1` = BandSolver only (default — band rebuilds around record fault clusters now include the incumbent's family; with it, JB470's orient-3 bands *re-exhaust with no gain* in ≤204k nodes = stronger optimality certificates), `2` = also Hunter DFS (smoke-tested: no throughput loss at 477M nodes/s, but widens tail branching — use when digging near a known double-break family), `0` = off.
- **Parity-class Hungarian move** (`--parity-pct`, default **2**%): `parity_move()` exactly reassigns an *entire chessboard-parity class* of interior non-clue cells (~97 cells — all pairwise non-adjacent so every neighbor is fixed) via the existing O(n³) `hungarian()`; per-entry random cost perturbation (total < one match unit, SCALE=2^14) makes equal-optimum ties into free sideways drift; ties applied at 30% like scatter. Status line: `parity=iters(+gains)`. Coverage no ≤16-cell region or ≤48-cell scatter can reach. Finding: **JB470, Jef_470 and their drift variants are already parity-LAP-optimal in both classes** (after==before always) — another certificate of deep local optimality; the move's value is drift + catching non-optimal parity classes on fresher lineages.
- **`--band-y0-min N`** (default 8 = old behavior): lowers the deepest band-rebuild start row (y0 ∈ [N,12], i.e. up to 14×14=196 freed cells at N=1). Motivation: σ-cycle finding — transitions between record basins are single indecomposable 80–154-cell cycles spanning rows 1–14, structurally out of reach of 42–98-cell bands and 25–40-cell umbrellas. Deep bands make completions needle-rare: pair with a much larger `--band-nodes` and expect an anytime lottery, not exhaustion.

## Exact CP-SAT tools (tools/, need `pip install ortools`)

- `window_solve.py`: fix all cells outside a window (rect / fault-dilation / explicit cells / `--board2` disagreement diff), optimally reassign the window's pieces (rotations free, rim grey-out enforced, clues pinned) via CP-SAT. **`--improve-only` is the workhorse**: pure feasibility ("any strictly better arrangement?") — often decides in *seconds* where maximize mode stays UNKNOWN for 25+ min. Any SAT on a record board = new world record; INFEASIBLE = proof of window-local optimality.
- `window_sweep.py`: resumable sweep of all WxH windows touching ≥1 fault (state file skips decided windows). Windows mixing faults + perfect territory = exhaustive piece-inflow tests. The overnight tool.
- `plateau_merge.py`: pairs same-score drift snapshots (from `--drift-dir`), exact-solves their disagreement sets (typically 4–35 cells) — merges independent plateau discoveries; dedupe state per pair.

**Findings so far** (JB470 = `runs/best_c1.txt`, R461 = `runs/seed_5clue_461.txt`):
- JB470: 10 faults in rows 1–5 cols 5–15 (3 clusters); left cluster **proven unimprovable** (42-cell radius-2 window, INFEASIBLE in 3.5 s); right cluster (56-cell radius-2) UNKNOWN after 30 min improve-only; 18-cell and 62-cell fault windows: no improvement found (62c undecided after 25 min maximize). R461: 19 faults, 16 in rows 11–15, 3 touch clue cells; two bands exhausted by BandSolver with no gain; 18-cell top cluster: no local repair. Both records are *deeply locally optimal* — +1 most likely needs piece inflow (sweep windows mixing perfect cells) or family leaps (umbrella) + plateau merges.
- **Cross-lineage elite boards agree on only 5–12/256 cells** (measured on history_c5) — no global backbone, classic genetic crossover is void for E2. Same-lineage drift variants agree on 220–250/256 → merge their small disagreement sets instead (plateau_merge).
- History logs only record new bests: equal-score drift never appears there (the seven "417" entries were one identical board re-logged on resume). Use `--drift-dir` to harvest actual variants.
- **2026-07-06 family-board survey** (all boards in `reference/bucas_init.js`, extracted/verified via e2lib): the two public 470s (Joshua_Blackwood_470 = our best_c1, JBlackwood+Jef_470) **disagree on 250/256 cells in the same orientation** (both pin clue 139@135r0) — genuinely independent basins, *not* near-siblings; direct exact merge is impossible (σ-cycle finding: cross-basin transitions are single indecomposable 80–154-cell cycles). No `Jef_470b` exists on the live site (checked 2026-07-06; only addition vs our snapshot is Louis_Verhaard_467_c). All 469s/468/467s differ from JB470 at ~250 cells too. Exception: `JBlackwood+PMcGavin_469` vs `_a` differ at just 2 cells ((4,9),(4,11)); their 35-cell dilate-2 improve-only window = UNKNOWN after 300 s (and a win would only be a second 470, not a record). **Jef_470 saved as `runs/seed_JBlackwood_Jef_470.txt`** (std letters, loads at 470, conforms): an independent second 470 lineage — polish/drift it separately and plateau_merge within its own basin. Its fault geometry: 19 fault cells rows 0–5 cols 5–13 (2 clusters), one 2-mismatch cell at (1,12), *no* N&W double-break cell. CP-SAT improve-only probes (2026-07-06): bare 19-cell fault window **INFEASIBLE in 0.2 s**; radius-1 all-fault window (56c) UNKNOWN @600 s; right cluster radius-2 (40c, cols 9–15 rows 0–5) UNKNOWN @600 s; left cluster radius-2 (62c, cols 3–11 rows 0–7) UNKNOWN @600 s → Jef_470 is the prime `window_sweep.py` overnight target (windows never probed by anyone; any SAT = 471 world record). R461 band re-sweep under `--cost2 1`: no gain, but orient-1 y0=11–12 and orient-2 y0=9–12 bands now **exhaust** (22k–20M nodes) = proven band-optimal *including* double-break moves; the 16-fault orient-0 bottom band still caps at 30M undecided.

- **2026-07-08 new 5-clue record 464** (forum post; all three published boards e2lib-verified at 464, clean multiset, 5 clues, 0 border errors). Poster's provenance: their 462–464 line comes from a *separate* 463 basin (~250 cells from their own 461), and they harvest distinct same-score boards at scale (10×464, 290×463, 2213×462) — mass same-basin drift + climb, validating our `--drift-dir` + `plateau_merge` design (our per-thread ring of 64 snapshots is tiny by comparison — consider harvesting far more). Board structure: the post's "Best" board (basin A, `seed_5clue_464.txt`) vs #2/#3 (basin B, `_v2`/`_v3`) differ at 250/256 cells; v2 vs v3 differ at just **6 cells** ((9,1),(11,2),(7,3),(10,3),(11,3),(11,4)). **All faults in all three boards live in rows 0–5** (bottom 10 rows perfect) → rotate 180° puts the whole fault zone in deep-band range, and fault+perfect mixed windows abound. Double-breaks: basin A has one at (13,4); v2 has two ((7,3),(10,3)); v3 none. CP-SAT improve-only probes (2026-07-08): v2×v3 6-cell merge INFEASIBLE 0.1 s; merge dilate-1 (32c) UNKNOWN @300 s; basin A bare fault window (27c) INFEASIBLE 0.3 s; v2 bare (26c) INFEASIBLE 0.4 s; **v3 bare fault window (25c, 27 win_edges) UNKNOWN even @540 s ×16 workers — the first bare-fault window anywhere that resists an optimality proof; prime +1 target** (any SAT = 465 world record). Next moves: long/overnight `window_sweep.py` on v3; solver hunt seeded from v3 (`--seed-edges runs\seed_5clue_464_v3.txt`, band rebuilds with rotation + `--cost2`); drift-harvest v2+v3 basin and plateau_merge within it.

**Scarce-color candidate ordering** (`Buckets::build`): candidates in each `exact[w][n]`/`by_w`/`by_n` bucket are stable-sorted (after the randomizing shuffle) by a static per-color rarity weight (`COLOR_RARITY[23]`, computed once in `init_static` from interior-piece color counts), so pieces carrying scarcer colors on their still-open sides are tried before common ones. Came from investigating [Joshua Blackwood's C# solver](https://github.com/jblackwood345/EternityII_Solver) (pure randomized DFS, no LNS — its main tricks are a fixed per-depth break-budget schedule and a 3-color cumulative-count prune). **Important finding**: our own piece set has colors 1–5 appearing only on border/corner pieces (0 interior occurrences, confirmed via `tools/e2lib.py` frequency count) — these are *already* handled exactly by the existing `D[]`/`avail[]` per-color deficit bound in `Hunter`, so porting Blackwood's hard depth-schedule prune directly would have been redundant/unsound. The remaining interior colors (6–22) only vary ~10% in scarcity — too weak to justify a hard cutoff, so a soft value-ordering heuristic was implemented instead of a prune. Smoke-tested (45 s side-by-side, `--clues 5 --mode hunt`): no throughput regression, comparable/slightly better avgdepth; real score impact needs a long user-run to confirm. JV/Hungarian algorithm was also evaluated for the polisher's `scatter_move` LAP — not adopted, since n≤48 there is already sub-millisecond and JV's practical speed edge only matters at n in the hundreds+.

## E2 ground truth

Validated against the e2.bucas.name viewer source in `reference/bucas_init.js`; regenerate data with `tools/make_data.py`, library `tools/e2lib.py`.

- 256 pieces (4 corner / 56 edge / 196 interior), 16×16 board, 22 colors + grey(0). Score = matched internal edges, max 480.
- **Bucas URL encoding**: per-cell 4 letters Up,Right,Down,Left, `'a'+motif`, row-major from top-left. Rotation cw = last char to front: `QUAD[p][k][i] = def[(i-k)&3]`. Format: `https://e2.bucas.name/#puzzle=NAME&board_w=16&board_h=16&board_edges=<1024 letters>&board_pieces=<256×3 digits>&show_conflicts=1`.
- **Clue placements** (0-based row-major cells; pieces 1-based): piece 208 @ cell 34 rot 1; 255 @ 45 rot 3; 139 @ 135 rot 0; 181 @ 210 rot 3; 249 @ 221 rot 1. 1-clue mode = piece 139 @ 135 rot 0 only.
- **Letter maps** to standard letters: jef→std = `atojeqlgbupkfrmhcwvsnid`, jbw→std = `abglqejotchmrfkpudinsvw` (derive with `e2lib.derive_letter_map`).
- Reference boards in `reference/bucas_init.js`: Joshua_Blackwood_470 (honors the center clue → valid 1-clue seed, only clue #3 of 5), Jef_470 (stored under compound key `JBlackwood+Jef_470`), the full 469 family (`JBlackwood+PMcGavin_469`, `_a`, `JBlackwood+Jef_469_a…g`), Joshua_Blackwood_468, Louis_Verhaard_467 (+`_a`,`_b`). All score exactly as named under `e2lib.score_edges`, and **every one of them honors the center clue** (139@135r0) → all are valid 1-clue boards. None honors all 5 clues.
- e2lib API gotchas: `score_edges(edges_str)` takes the 1024-char std-letter string (returns `(score, conflicts, border_errors)`); `check_piece_set(data, edges)` returns placements and raises on duplicates; `parse_url(url)` returns the raw params dict (get `params["board_edges"]`).
